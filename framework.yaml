# DRAFT — NOT FOR PRODUCTION.
# The concord-controlpack-fedramp control set is unauthored (TODO-placeholder
# descriptions), fails `concord control lint`, and is intentionally NOT
# published to the registry. This manifest is a roadmap placeholder; do not
# present FedRAMP as an available framework until the pack is authored with
# domain review (assessment doc 31 §7). The control_packs ref below points at
# an artifact that does not yet exist in the registry.
apiVersion: concord.dev/v1
kind: Framework
metadata:
  id: fedramp
  name: "FedRAMP Moderate (DRAFT — unauthored)"
  version: v0.1.0
  description: "DRAFT / NOT FOR PRODUCTION — FedRAMP Moderate Baseline (NIST 800-53 Rev 5 + FedRAMP overlays). Controls are unauthored stubs; not published to the registry."
  authors: ["concord-dev"]
  attribution:
    sources:
      - usnistgov-oscal-content-fedramp
      - gsa-fedramp-baselines

spec:
  control_packs:
    - source: ghcr.io/concord-dev/concord-controlpack-fedramp
      version: ">=v0.1.0,<v1.0.0"
      reason: "FedRAMP Moderate baseline controls drawn from NIST OSCAL"
